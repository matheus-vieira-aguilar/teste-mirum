<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}

            <a href="{{ path('products_index') }}">PRODUCTS</a>
            <a href="{{ path('category_index') }}">CATEGORIES</a>
            <a href="{{ path('store_index') }}">STORES</a>

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
